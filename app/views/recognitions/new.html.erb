<% provide(:title, "画像アップロード") %>
<div class="container mx-auto py-12 px-6 text-center">
  <h2 class="text-3xl font-bold text-white mb-4">芸能人のアイテムをチェック</h2>
  <p class="text-white mb-8">画像をアップロードして、お気に入りの芸能人の関連商品を見つけましょう。</p>

  <div class="bg-white rounded-2xl shadow-lg p-6 max-w-md mx-auto">
    <%= form_with model: @recognition, url: recognitions_path, multipart: true, data: { turbo: false } do |f| %>
      <% if @recognition.errors.any? %>
        <div class="mb-4">
          <ul class="text-red-500">
            <% @recognition.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>
      
      <div class="w-full min-h-60 bg-gray-100 flex items-center justify-center rounded-lg mb-4 overflow-hidden">
        <img id="image-preview" src="#" alt="プレビュー画像" class="hidden w-full h-full rounded-lg shadow-md object-cover" />
        <span class="text-gray-500">プレビュー</span>
      </div>

      <div class="flex flex-col justify-center md:flex-row md:space-x-4 w-full">
        <button type="button" id="select-image-btn" class="w-full md:w-auto bg-gray-300 text-gray-700 px-4 py-2 rounded-lg hover:bg-gray-400 mb-4 md:mb-0">
          画像を選択
        </button>
        <%= f.hidden_field :dummy, value: "1" %>
        <%= f.file_field :image, id: "image-upload", class: "hidden", accept: "image/png,image/jpeg" %>

        <%= f.submit "アップロード", class: "w-full md:w-auto bg-blue-500 text-white py-2 px-4 rounded-lg hover:bg-blue-600 cursor-pointer" %>
      </div>
    <% end %>
  </div>
</div>